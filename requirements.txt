pandas>=2.0.0
openpyxl>=3.1.0
# scikit-learn fixado na versão usada para treinar os modelos .pkl —
# versão diferente no deploy quebra o carregamento dos modelos
scikit-learn==1.8.0
joblib>=1.5.0
streamlit>=1.30.0
plotly>=5.18.0
geopy>=2.4.0
fpdf2>=2.8.0

# Hub de Municípios — consulta ao SICONFI e leitura dos agregados da BDGD.
# Ambas são dependências puras (wheels padrão) e não afetam os demais módulos.
requests>=2.31.0
pyarrow>=14.0.0

# Trilha de uso (pacote `acesso`). Só são exercidas quando o backend Google Sheets
# está configurado em st.secrets; sem elas o log cai para CSV local automaticamente.
# Ambas são puras e não tocam o resolvedor de scikit-learn/joblib.
gspread>=6.0.0
google-auth>=2.28.0

# Apresentação comercial da carteira (.pptx) — Hub de Municípios. Dependência pura,
# sem binário nativo, e não toca o resolvedor de scikit-learn/joblib. O gerador desenha
# tudo em vetor (inclusive o mapa), justamente para NÃO precisar do kaleido, que pesa
# ~100 MB e só serviria para rasterizar figura do Plotly.
python-pptx>=1.0.0
